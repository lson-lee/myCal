name: auto-publish-lean-cloud
on:
  push:
    branches: [ main ]
jobs:
  go-publish:
    runs-on: ubuntu-latest
    environment: test
    steps:
      - name: 冲鸭
        run: curl ${{ secrets.LEANCLOUD_PUBLISH }} -X POST